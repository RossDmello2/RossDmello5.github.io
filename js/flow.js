/* ============================================================
   ROSS DMELLO — FLOW ENGINE (v2.1)
   The traveling packet dots on project diagrams were retired by
   design decision: the connections now read as flowing orange
   dotted lines only (the `wire-flow` dash animation in CSS
   carries all diagram motion — see css/styles.css + blueprint.css).
   The philosophy pipeline simulation in js/engines.js is
   unaffected and keeps its live packets.
   This file is kept so index.html needs no edit; it does nothing.
   ============================================================ */
(function () {
	'use strict';
	/* intentionally empty */
})();
